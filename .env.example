# Copy to .env.local and fill in to enable cross-device sync.
# Get these from your Supabase project: Settings -> API.
# Leaving these unset runs Twine local-only, with no sign-in gate.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
